$kind: http-request
name: List Items
method: GET
url: "{{baseUrl}}/item?limit={{limit}}"
order: 1000
headers:
  - key: session-token
    value: "{{sessionToken}}"
queryParams:
  - key: limit
    value: "{{limit}}"
  - key: offset
    value: "{{offset}}"
    disabled: true
