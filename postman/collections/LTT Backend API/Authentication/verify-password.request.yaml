$kind: http-request
name: Verify Password
method: POST
url: '{{baseUrl}}/verify-password'
order: 5000
headers:
  - key: Content-Type
    value: application/json
  - key: session-token
    value: '{{sessionToken}}'
body:
  type: json
  content: |-
    {
      "password": "password123"
    }
